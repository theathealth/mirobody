FROM theta-public-registry.cn-hangzhou.cr.aliyuncs.com/theta/mirobody-backend

WORKDIR /app

COPY requirements.txt .
RUN --mount=type=cache,target=/root/.cache/pip \
    pip install -r requirements.txt && \
    pip list

